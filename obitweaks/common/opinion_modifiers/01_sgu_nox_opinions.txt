triggered_opinion_nox_ancient = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_nox
		FROM = {
			has_trait = trait_ancient
		}
	}

	opinion = {
		base = 250
	}
}

triggered_opinion_nox_asgard = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_nox
		FROM = {
			has_trait = trait_asgard
		}
	}

	opinion = {
		base = 250
	}
}

triggered_opinion_nox_ori = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_nox
		FROM = {
			has_trait = trait_ori
		}
	}

	opinion = {
		base = 100
	}
}

triggered_opinion_nox_tauri = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_nox
		FROM = {
			has_trait = trait_tauri
		}
	}

	opinion = {
		base = 100
	}
}

triggered_opinion_nox_wraith = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_nox
		FROM = {
			has_trait = trait_wraith
		}
	}

	opinion = {
		base = 100
	}
}

triggered_opinion_nox_goauld = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_nox
		FROM = {
			has_trait = trait_goauld
		}
	}

	opinion = {
		base = 100
	}
}

triggered_opinion_nox_jaffa = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_nox
		FROM = {
			has_trait = trait_jaffa
		}
	}

	opinion = {
		base = 100
	}
}

triggered_opinion_nox_replicator = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_nox
		FROM = {
			has_trait = trait_replicator
		}
	}

	opinion = {
		base = -1000
	}
}

triggered_opinion_nox_asuran = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_nox
		FROM = {
			has_trait = trait_asuran
		}
	}

	opinion = {
		base = -1000
	}
}

triggered_opinion_nox_nox = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_nox
		FROM = {
			has_trait = trait_nox
		}
	}

	opinion = {
		base = 500
	}
}

