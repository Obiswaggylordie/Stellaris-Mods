triggered_opinion_replicator_ancient = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_replicator
		FROM = {
			has_trait = trait_ancient
		}
	}

	opinion = {
		base = -1000
	}
}

triggered_opinion_replicator_asgard = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_replicator
		FROM = {
			has_trait = trait_asgard
		}
	}

	opinion = {
		base = -1000
	}
}

triggered_opinion_replicator_ori = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_replicator
		FROM = {
			has_trait = trait_ori
		}
	}

	opinion = {
		base = -1000
	}
}

triggered_opinion_replicator_tauri = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_replicator
		FROM = {
			has_trait = trait_tauri
		}
	}

	opinion = {
		base = -1000
	}
}

triggered_opinion_replicator_wraith = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_replicator
		FROM = {
			has_trait = trait_wraith
		}
	}

	opinion = {
		base = -1000
	}
}

triggered_opinion_replicator_goauld = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_replicator
		FROM = {
			has_trait = trait_goauld
		}
	}

	opinion = {
		base = -1000
	}
}

triggered_opinion_replicator_jaffa = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_replicator
		FROM = {
			has_trait = trait_jaffa
		}
	}

	opinion = {
		base = -1000
	}
}

triggered_opinion_replicator_replicator = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_replicator
		FROM = {
			has_trait = trait_replicator
		}
	}

	opinion = {
		base = 500
	}
}

triggered_opinion_replicator_asuran = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_replicator
		FROM = {
			has_trait = trait_asuran
		}
	}

	opinion = {
		base = 50
	}
}

triggered_opinion_replicator_nox = {
	trigger = {
		NOT = { is_country_type = fallen_empire }
		has_trait = trait_replicator
		FROM = {
			has_trait = trait_nox
		}
	}

	opinion = {
		base = -1000
	}
}

